

## FieldLog Rename — Implementation Plan

### Phase 1: User-Facing Copy (4 files)

**index.html** — Lines 7, 8, 20, 21, 22, 23
- `Site Log` → `FieldLog`
- `Site Log App` → `FieldLog App`

**src/pages/Index.tsx** — Line with `SiteLog` in header h1
- `SiteLog` → `FieldLog`

**src/components/EmailEntry.tsx** — Line with `SiteLog` in h1
- `SiteLog` → `FieldLog`

**src/pages/ResetPassword.tsx** — 3 occurrences of `SiteLog` in headings/copy
- All `SiteLog` → `FieldLog`
- "Return to the SiteLog app" → "Return to the FieldLog app"

### Phase 2: PDF / Export / Email (3 files)

**supabase/functions/generate-report/index.ts**
- Line 78: `"Generated by SiteLog"` → `"Generated by FieldLog"`
- Line 451: `SiteLog Daily Report` → `FieldLog Daily Report`
- Line 579: `SiteLog Daily Report` → `FieldLog Daily Report`

**supabase/functions/regenerate-report-pdf/index.ts**
- Line 66: `"Generated by SiteLog"` → `"Generated by FieldLog"`
- Line 220: `SiteLog Daily Report` → `FieldLog Daily Report`

**src/components/ReportSection.tsx**
- Line with `SiteLog Daily Report` in email subject → `FieldLog Daily Report`

### Phase 3: URL / Auth Redirects (2 files)

**supabase/config.toml**
- `site-voice-log.lovable.app` → `fieldlog.lovable.app` (site_url and redirect URLs)

**src/hooks/useUser.ts**
- Line 61: `site-voice-log.lovable.app` → `fieldlog.lovable.app`

### Phase 4: Preserved (No Change)

- `sitelog_onboarding_seen` localStorage key — unchanged
- No database/bucket/migration renames

### Phase 5: Post-Implementation

Deploy edge functions (`generate-report`, `regenerate-report-pdf`) after code changes. Return full change report with break-risk assessment.

### Risk Notes

- The new published URL `fieldlog.lovable.app` must be configured via Project Settings → Domains after code changes, or existing `site-voice-log.lovable.app` will still be the live URL
- Password reset redirect will point to new domain — must ensure domain is active before testing

### Files Changed Summary

| File | Changes |
|------|---------|
| `index.html` | 6 meta tag updates |
| `src/pages/Index.tsx` | 1 brand text |
| `src/components/EmailEntry.tsx` | 1 brand text |
| `src/pages/ResetPassword.tsx` | 3 brand texts |
| `supabase/functions/generate-report/index.ts` | 3 strings |
| `supabase/functions/regenerate-report-pdf/index.ts` | 2 strings |
| `src/components/ReportSection.tsx` | 1 email subject |
| `supabase/config.toml` | 2 URL references |
| `src/hooks/useUser.ts` | 1 redirect URL |

Total: **9 files**, ~20 string replacements. Edge functions redeployed after.

